# Consulting page revision

- Replace the current triangular service map with the specified two-column pipeline: three stacked stage plates on the left and one fixed detail panel on the right.
- Keep `/consulting` in `useWorld('academic')`, reusing the existing navigation, footer, Fraunces/Geist Mono typography, and Academic color tokens without changing other pages.
- Select **02 Midstream** on load; stage clicks update only the right panel, with no panel transition so the connector glint remains the page’s sole motion.
- Build the pipeline connector in CSS: a permanently visible dashed track plus two brief staggered glints; reduced-motion keeps only the static track.
- Tighten the supplied copy only where repetition costs attention; preserve every required panel element and omit Evidence entirely for 01 and 03 because none was supplied.
- Place a single compact “Describe a decision” link directly below the split that jumps to the intake form; this avoids duplicating the form while honoring both CTA placements.
- Add the one-line “How I work” thesis band, retain the existing fixed three-item writing rail, then show the four-field email intake and footer.
- Keep the existing quiet homepage and navigation links and sitemap entry; do not touch the landing folios, Resume, Portfolio, modes, or tokens.
- The local reference file is unavailable in this environment, so I will match the described feel and validate desktop, 390px mobile, stage switching, reduced motion, and no layout shift visually.
